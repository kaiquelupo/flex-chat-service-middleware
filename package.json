{
  "name": "serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "crypto": "^1.0.1",
    "querystring": "^0.2.0"
  },
  "devDependencies": {
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  }
}
